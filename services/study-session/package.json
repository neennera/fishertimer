{
  "name": "@fishertimer/study-session-service",
  "version": "0.1.0",
  "private": true,
  "description": "Study session room lifecycle, participant rosters, and room capacity",
  "scripts": {
    "dev": "go run cmd/main.go",
    "build": "go build -o bin/server cmd/main.go",
    "test": "go test ./...",
    "lint": "go vet ./...",
    "clean": "go clean"
  }
}