{
  "name": "@fishertimer/study-timer-service",
  "version": "0.1.0",
  "private": true,
  "description": "Independent user timer execution, focus cycles, and rest intervals",
  "scripts": {
    "dev": "go run cmd/main.go",
    "build": "go build -o bin/server cmd/main.go",
    "test": "go test ./...",
    "lint": "go vet ./...",
    "clean": "go clean"
  }
}